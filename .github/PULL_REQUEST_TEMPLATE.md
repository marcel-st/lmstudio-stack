## What does this change?

<!-- Brief description of what this PR does and why -->

## How was it tested?

<!-- Describe how you verified the change works, e.g.:
- Ran `docker compose up -d` and confirmed all services healthy
- Ran the verification commands from the README
- Tested context size reload with scripts/reload-context.sh
-->

## Checklist

- [ ] Tested against a running stack (`docker compose ps` shows all services healthy)
- [ ] `.env` is not committed (check `git status`)
- [ ] README updated if setup or usage changed
- [ ] `PLAN.md` updated if architecture changed
